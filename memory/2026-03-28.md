# AutoMarket Slovenia - Development Log

## 2026-03-28

### Key Changes Made:

#### 1. AddCarPage Fixes
- Added `fuel_type` and `body_type` to car data objects for proper DB storage
- Fixed car detail page to show Fuel Type and Body Type properly

#### 2. API Actions Fixed
- `deleteCar` now uses `action: 'delete'` instead of 'remove'
- `updateCar` now uses `action: 'update'` instead of 'edit'
- Added refresh after delete in DashboardPage

#### 3. Boost Packages Lowercase
- Changed from UPPERCASE to sentence case:
  - "PAKET VSEH CEN" → "Paket vseh cen"
  - "TOP IZBIRA" → "Top izbira"
  - "SKOK NA VRH" → "Skok na vrh"
- Applied to: DashboardPage, AddCarPage, SellPage, AdminPage

#### 4. SellPage Fixes
- All package names and subtitles now in lowercase
- Fixed translations for SL/EN

#### 5. Language Support
- Added `useLanguage` hook to AddCarPage
- All form labels now use `t()` function
- Supported fields: Title, Brand, Model, Year, Price, Mileage, Engine, HP, Fuel Type, Transmission, Body Type, Color, City, Photos, Description

#### 6. Admin Panel
- Created `/api/admin.php` with full CRUD operations:
  - Users management (list, detail, update, delete)
  - Cars management (list, detail, update, delete)
  - Packages CRUD
  - Analytics (stats, revenue, views)
  - Purchases history
  - Messages
  - Settings
- Created demo data for fallback when API unavailable
- Added Recharts for visualization

#### 7. Admin Login
- Fixed login page with proper credentials check
- Credentials: admin@automarket.si / admin123
- Added proper navigation to /admin-panel

#### 8. Database Tables
- Created SQL schema for:
  - `site_settings` - Site configuration
  - `packages` - Package management
  - `car_views` - Analytics views

#### 9. Backup Location
- Full project backup: `C:\Users\andii\OneDrive\Desktop\car-marketplace-backup\2026-03-28_01-05-12`

### Issues to Fix:
- Boost packages in AddCarPage don't have full UI yet
- Premium photo badges need implementation
- Some old sell page still exists (needs removal)
- Payment flow for packages not fully integrated

### Files Modified:
- `/src/pages/AddCarPage.jsx` - Multiple fixes
- `/src/pages/DashboardPage.jsx` - Delete refresh
- `/src/pages/AdminPage.jsx` - Full rewrite with API + demo
- `/src/pages/AdminLoginPage.jsx` - Fixed login
- `/src/pages/SellPage.jsx` - Lowercase fix
- `/api/admin.php` - New admin API
- `/src/lib/LanguageContext.jsx` - Added translations
- `/src/App.jsx` - Route changes (/admin-login, /admin-panel)